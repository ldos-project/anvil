# Steps to run
0. Select an example to run: `ln -s ./code_snippets/pass/simple.cpp LLMCode.h`
1. Run `make clean && make -j`.
2. Run `./template` - this will always run error-free, even when properties we desire are being violated.